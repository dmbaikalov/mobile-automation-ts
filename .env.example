TEST_ENV="local"
BROWSERSTACK_USERNAME=""
BROWSERSTACK_ACCESS_KEY=""
EMULATOR_ID=""
ANDROID_APP_PATH="apps/android.wdio.native.app.v2.2.0.apk"
IOS_APP_PATH="apps/ios.simulator.wdio.native.app.v2.2.0.zip"

# bs://<hash> values returned by the BrowserStack App Automate upload API
# (https://api-cloud.browserstack.com/app-automate/upload). Re-upload and
# update these whenever the underlying APK/IPA changes — the hash isn't
# stable across uploads.
BROWSERSTACK_ANDROID_APP_URL=""
BROWSERSTACK_IOS_APP_URL=""
